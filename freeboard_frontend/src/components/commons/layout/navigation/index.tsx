import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 30px;
  background-color: #ffd600;
`;

export default function LayoutNavigation() {
  return <Wrapper>자유게시판, 중고마켓 같은 메뉴 작성</Wrapper>;
}
